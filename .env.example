# ── Ollama Cloud (LLM + Expert reasoning) ───────────────────────────────────
OLLAMA_API_KEY=your-ollama-cloud-api-key
OLLAMA_BASE_URL=https://ollama.com/v1
OLLAMA_MODEL=glm5.1

# ── Faster-whisper STT (local) ──────────────────────────────────────────────
# Run: docker run -d --name faster-whisper-server -p 8000:8000 \
#        -e WHISPER__MODEL=large-v3-turbo -e WHISPER__COMPUTE_TYPE=int8 \
#        fedirz/faster-whisper-server:latest-cpu
FWHISPER_BASE_URL=http://localhost:8000/v1
FWHISPER_API_KEY=unused

# ── LiveKit (get from cloud.livekit.io → free tier) ──────────────────────────
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=APIxxxxxxxxxxxxxxxxx
LIVEKIT_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Edge-TTS voice (no API key needed — free Microsoft neural voices) ─────────
EDGE_TTS_VOICE=en-GB-RyanNeural

# ── Supabase PostgreSQL ──────────────────────────────────────────────────────
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres

# ── Backend ───────────────────────────────────────────────────────────────────
PORT=3000
AGENT_SECRET=jarvis-agent-2024
BACKEND_URL=http://localhost:3000